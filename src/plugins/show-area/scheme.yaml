- areas: false

- areas:
    - onClickAny: # Обрабатывает клик при клике на любой области
        $: onClickAny
    - onClickOut: # Обратывает клик в любом месте кроме любой области
        $: onClickOut
    - top: 250 # Отступ сверху в px
      left: 10 # Отступ слева в px
      width: 100 # Ширина области слева в px
      height: 100 # Высота области слева в px
      border: true # Показывать рамку вокруг области true/false/color
      onClick: # Обрабатывает клик по области
        $: Area1
    - top: 10
      left: 10      
      width: 50
      height: 50
      border: '#e97616'